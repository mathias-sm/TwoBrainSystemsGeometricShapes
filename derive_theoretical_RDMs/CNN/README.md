# Generate RDMs for CNNs models:

`python distances.py --model=<model>` will generate the RDMs for each relevant
layers of model `<model>` in the output folder.

`<model>` takes values in `cornet_s`, `resned` and `densenet`
